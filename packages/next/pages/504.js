import Layout from "components/layout";
import PageNotFound from "components/pageNotFound";

export default function Page505({}) {
  return (
    <Layout>
      <PageNotFound code="505" />
    </Layout>
  );
}
